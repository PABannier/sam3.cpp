[project]
name = "sam3-cpp"
version = "0.1.0"
description = "Weight converters for sam3.cpp"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0",
    "numpy>=1.24",
]

[tool.uv]
package = false
